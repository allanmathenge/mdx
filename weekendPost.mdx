---
title: 'Hello world, its a weekemd'
date: '2023-011-10'
tags: ['next.js', 'nextjs', 'video']
---

<Video id="tjeaZDavPt4" />

# Weekend post

We recommend using static Generation with or without data

```ts
const sum = (num1: number, num2: number): number => {
    return num1 + num2
}
console.log(sum(6, 4)) // 10
```

We can use static generation for many types of pages.